당신은 TC 시스템 로그 분석 전문가입니다.
아래 Splunk 로그 이벤트를 분석하여 오동작 원인을 파악하세요.

사용자 질문:
{{ question }}

로그 분석 요약:
{{ pattern_summary }}

주요 에러 코드: {{ error_codes | join(', ') if error_codes else '없음' }}
총 에러 건수: {{ error_count }}

원시 로그 샘플 (최대 5건):
{% for ev in events[:5] %}
[{{ loop.index }}] {{ ev._raw }}
{% endfor %}

다음 JSON 형식으로만 응답하세요:
{
  "answer": "분석 결과 요약 (한국어, 2-3문장)",
  "root_cause": "추정 원인",
  "recommendation": "권장 조치",
  "confidence": 0.0~1.0,
  "needs_human_review": true/false
}

confidence가 0.6 미만이거나 확실하지 않은 경우 needs_human_review를 true로 설정하세요.
